<UserControl x:Class="ProjectManager.WPF.Views.MainAppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectManager.WPF.Views"
             xmlns:controls="clr-namespace:ProjectManager.WPF.Controls"
             xmlns:viewModels="clr-namespace:ProjectManager.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <viewModels:MainAppViewModel x:Key="ViewModel"/>
    </UserControl.Resources>

    <Grid DataContext="{StaticResource ResourceKey=ViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="10,0">
            <controls:ProjectSelection/>
        </StackPanel>

        <local:ProjectView Grid.Column="1"/>
    </Grid>
</UserControl>
